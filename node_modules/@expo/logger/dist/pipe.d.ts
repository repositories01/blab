/// <reference types="node" />
import { Readable } from 'stream';
import bunyan from '@expo/bunyan';
declare type LineLogger = (line: string) => void;
declare type LineTransformer = (line: string) => string | null;
interface SpawnOutput {
    stdout?: Readable | null;
    stderr?: Readable | null;
}
interface PipeOptions {
    printAsStdoutOnly?: boolean;
    lineTransformer?: LineTransformer;
}
declare function pipe(stream: Readable, loggerFn: LineLogger, lineTransformer?: LineTransformer): void;
declare function pipeSpawnOutput(logger: bunyan, { stdout, stderr }?: SpawnOutput, { printAsStdoutOnly, lineTransformer }?: PipeOptions): void;
export { pipe, pipeSpawnOutput };
